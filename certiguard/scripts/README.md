# Scripts

| Script | Purpose |
|--------|---------|
| [`smoke_dashboard.py`](smoke_dashboard.py) | Starts the Flask dashboard against `demo_runs/smoke_audit.log` (creates file if missing). Requires `npm run build` in `src/certiguard/ui`. |

Run from repo root: `python scripts/smoke_dashboard.py`
